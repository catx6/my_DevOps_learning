go - это яп компилируемый, многопоточный, используется для высоконагруженых систем
gc - основной компилятор
- написан на go раньше на C
- сейчас Go компилирует сам себя
gccgo - реализация Go в составе GCC
- написана на c++

#### особенности golang
- простота
мало ключевых слов
нет классов
нет наследования
нет перегрузок
нет generics
- высокая производительность
быстрый компилятор
втроенный garbage collector
статическая типизация
простая модель памяти
- втроенная многопоточность
goroutinas и channels
легкий поток ~2КБ, который Go-runtime сам распределяет по ядрам
- мощная стандартная библиотека
HTTP
шаблоны
работа с сетью
JSON/XML
хеширование шифрование
профилирование
- статически собранный бинарник
Go собирает программу в единый файл
без зависимостей
без виртуальных машин
без интерпритаторов
- сильная экосистема
GO-основной язык для:
Docker
Kubernetes
Terraform
Prometheus
Grafana
Consul
Etcd
ВЕСЬ cloud-native мир построен на Go
- безопасность типов
Go статически типизирован
есть как **автоматическое** типизирование так и ручное
- минимум магии и скрытого поведения
нет скрытых конструкторов
нет автоматических преобразований
код всегда читаем
форматирование - стандарт `gofmt`

---

### Основные знания Go для backend-разработчика

## **1. Язык Go — твёрдая база**

### Синтаксис и конструкции:

- типы данных (int, string, struct, map, slice)
    
- указатели
    
- методы и интерфейсы
    
- встраивание (embedding)
    
- пакеты, модули, go.mod / go.sum
    

### Ошибки:

- `error` как интерфейс
    
- паттерны ошибок
    
- кастомные ошибки
    
- оборачивание: `fmt.Errorf("%w", err)`
    

---

# ⚙️ 2. Горутины и конкурентность

Go обожают за его параллелизм, поэтому MUST KNOW:

- goroutines (`go func()`)
    
- каналы (буферизированные/небуферизированные)
    
- `select`
    
- `sync.Mutex`, `sync.RWMutex`
    
- `sync.WaitGroup`
    
- worker pools
    
- context (очень важно!)
    

---

# 🌐 3. Работа с сетью и API

### Умение делать сервер:

- `net/http`
    
- маршрутизация
    
- middleware
    
- работа с JSON (`encoding/json`)
    
- graceful shutdown  
    → `http.Server.Shutdown(ctx)`
    

### Популярные фреймворки:

- **Gin** (чаще всего)
- **Echo**
- Fiber  
    Выучи хотя бы Gin.

---

#### 4. Работа с Базами Данных

### SQL:
- PostgreSQL (основной навык)
- MySQL / MariaDB (дополнительно)
- SQLite (для тестов)
### ORM / драйверы:

- `database/sql`
- **sqlx** (очень популярно)
- **GORM** (если хочешь удобства)

### Обязательно:
- миграции БД (goose, migrate)
- транзакции
- индексы, join’ы, нормализация    

---

#### 5. Архитектура и структура проекта

Что должен уметь backend на Go:
- чистая архитектура (Clean Architecture)
- слои: handler → service → repository
- зависимость через интерфейсы
- разбиение на модули / пакеты
- конфигурация (env, config.yaml)    

---

#### 6. Тестирование
В Go тесты пишут просто, но обязательно:
- `testing` пакет
- unit-тесты
- мокинг (`testify/mock`)
- интеграционные тесты
- тестирование HTTP

---

#### 7. Docker + Docker Compose

Backend никогда не живёт один.
Нужно уметь:
- создавать Dockerfile
- multi-stage build
- докеризация Go-сервисов
- запуск сервисов в Compose (БД, кэш, API)

---

#### 8. gRPC / Protobuf (по желанию, но очень важно)

Микросервисы на Go часто используют:
- Protocol Buffers
- gRPC-сервер, gRPC-клиент
- streaming
- codegen: `protoc`    

---

#### 9. RabbitMQ / Kafka / NATS (очень желаемо)

Для асинхронных систем:
- очереди
- event-driven architecture
- consumers / producers

---

#### 10. Observability
Чтобы понимать, что происходит в проде:
- логирование (zap, zerolog)
- tracing (OpenTelemetry)
- metrics (Prometheus)
- healthchecks

---

#### 11. DevOps-минимум

Не обязательно быть DevOps, но нужно понимать:
- Linux + systemd
- Bash
- CI/CD основы (GitHub Actions, GitLab CI)
- Kubernetes на уровне "запустить приложение"

---

#### 12. Паттерны Go

- dependency injection через интерфейсы
- repository/service
- worker pool
- fan-in / fan-out
- graceful shutdown
- adapter pattern

---

#### Итого — чтобы стать Go backend'ом, тебе нужно знать:

✔ сам язык  
✔ конкуррентность  
✔ работу с HTTP  
✔ базу данных  
✔ архитектуру проекта  
✔ Docker  
✔ тесты

---

#### как я буду учить golang
[[golang core]]


