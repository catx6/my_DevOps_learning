Docker - важная тема для [[DevOps]] инженера и я рассмотрю
- что такое докер
- что такое image, container, Dockerfile, Docker Engine, Docker Hub / Registry, Volume, Network, Compose
- как работает Docker
- как реализованна изоляция контейнеров namespace
- docker commit
- типы сетей в докере
- docker squash

-----

### что такое Docker

Docker - платформа контейнеризации, позволяющая запускать приложения в изолированных средах
контейнер - мини-виртуальная машина но гораздо ближе к ядру хост-системы

| понятие                   | описание                                                                           |
| ------------------------- | ---------------------------------------------------------------------------------- |
| [[Image]]                 | **шаблон** файловой системы и зависимостей для **контейнера**                      |
| [[Container]]             | запущеный экземпляр образа, хранит состояние, логи, конфиги                        |
| [[DockerFile]]            | скрипт описывающий сборку образа                                                   |
| [[Docker Engine]]         | Демон (`Dockerd`), управляющий контейнерами, образами, сетями, volume              |
| [[Docker Hub]] / Registry | Репозиторий для хранения и распространения образов                                 |
| [[ИНСТРУМЕНТАРИЙ/DOCKER/объекты/volume]]                | постоянное хранилище данных контейнеров. не удаляется при удалении контейнера      |
| [[Network]]               | виртуальная сеть, через которую контейнеры общаются друг с другом                  |
| [[Docker-Compose]]        | Инструмент для описания и запуска **нескольких контейнеров как одного приложения** |

----

### архитектура Docker

```architecture
+------------------------------------------------+
|                  Docker CLI                    |
+------------------------------------------------+
            ↓ API запросы
+------------------------------------------------+
|               Docker Daemon (dockerd)          |
|   ├── Управление контейнерами                  |
|   ├── Управление образами                      |
|   ├── Управление сетями и volume               |
+------------------------------------------------+
            ↓ взаимодействие с ядром Linux
+------------------------------------------------+
|           ОС и ядро Linux (cgroups, namespaces)|
+------------------------------------------------+
```

---

#### порядок изучения
1. [[Image]]
2. [[Container]]
3. [[Network]]
4. [[Docker Engine]]
5. [[DockerFile]]
6. [[Docker Hub]]
7. [[ИНСТРУМЕНТАРИЙ/DOCKER/объекты/volume]]
8. [[Docker-Compose]]


---

## 🧰 4. DevOps должен уметь:

### Работать с образами

- Собирать (`docker build`)
- Тегировать (`docker tag`)
- Версионировать (`:1.0`, `:prod`)
- Оптимизировать Dockerfile (multi-stage build, `.dockerignore`)
- Не использовать `latest`

### Управлять контейнерами
- Запускать (`docker run`, `compose up`)
- Настраивать сеть (`--network`, `bridge`, `host`)
- Лимитировать ресурсы (`--memory`, `--cpus`)
- Настраивать персистентные тома (`-v`, `docker volume create`)
- Логировать и дебажить (`docker logs`, `exec`, `inspect`)
### Использовать CI/CD
- Автоматическая сборка образов (GitLab CI, Jenkins)
- Push в приватный registry
- Проверка уязвимостей (`trivy`, `grype`)
- Деплой контейнеров в Kubernetes

-> всю практику записывать в [[практика Docker]]