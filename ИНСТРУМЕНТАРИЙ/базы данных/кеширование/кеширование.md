кэш - это промежуточное хранилище, которое хранит результат часто повторяющихся вычислений или запросов, чтобы не выполнять их заново

примеры:
- при загрузке сайтов браузер сохраняет CSS, JS и картинки в кэше, чтобы не качать их при каждом обновлении страницы
- [[redis]] хранит результаты SQL-запросов чтобы не обращаться к базе заново
- Linux кеширует файловую систему в Ram чтобы ускорить доступ к файлам

| Область              | Пример                                   | Где хранится           |
| -------------------- | ---------------------------------------- | ---------------------- |
| **ОС**               | Кеш страниц (page cache), буфер файлов   | RAM                    |
| **Браузеры**         | HTML, CSS, JS, изображения               | Локально (диск)        |
| **Базы данных**      | Query Cache, Prepared Statements         | RAM / внешние сервисы  |
| **Сети / CDN**       | Cloudflare, Akamai                       | Геораспределённые кэши |
| **Backend**          | Redis / Memcached / локальные объекты    | RAM или диск           |
| **DevOps и CI/CD**   | Кеш Docker слоёв, GitHub Actions cache   | диск/облако            |
| **Python / Ansible** | Кеш шаблонов, фактов и результатов задач | локальные файлы        |

#### виды кэшей

| Вид кеша           | Описание                                    | Пример                                  |
| ------------------ | ------------------------------------------- | --------------------------------------- |
| **Локальный**      | хранится в памяти или на диске сервера      | Python `functools.lru_cache`, кеш nginx |
| **Дисковый**       | данные на SSD/HDD                           | apt cache, docker layer cache           |
| **Распределённый** | общий кэш между несколькими узлами          | Redis, Memcached                        |
| **HTTP-кеш**       | хранение контента на уровне браузера/прокси | `Cache-Control`, `ETag`, `Expires`      |
| **БД-кеш**         | кэширование результатов запросов            | PostgreSQL shared buffers               |
| **DNS-кеш**        | хранит IP адреса доменов                    | systemd-resolved, bind                  |

## 🔧 Популярные инструменты кеширования

#### **Redis**
- Самый популярный кэш для backend и DevOps.
- Работает в памяти (RAM), поддерживает TTL (время жизни ключей).
- Поддерживает структуры: строки, списки, множества, хэши, JSON и Pub/Sub.
- Используется в Django, Flask, Kubernetes, CI/CD.
#### **Memcached**
- Очень быстрый, лёгкий, без постоянного хранения (всё в RAM).
- Подходит для простых key-value сценариев.
- Часто используется в PHP, WordPress, старых системах.    
#### **Varnish**
- HTTP reverse proxy cache.
- Ставится перед веб-сервером и кеширует HTML-страницы.
- Используется для ускорения сайтов и API.    
#### **[[nginx]] Cache**
- Может кешировать ответы от backend-а (FastCGI, proxy).
- Часто используется для API и статических сайтов. 
#### **Docker Build Cache**
- Хранит слои образов (layer cache).
- Позволяет ускорять пересборку контейнеров.
#### **CDN Cache (Cloudflare, Akamai, [[AWS]] CloudFront)**
- Кешируют контент на краевых серверах (edge).
- Уменьшают нагрузку и ускоряют доставку пользователям.

