inode - структура данных хранящая метаднные о файле
[[файловая система]]

### 🔹 Что хранится в inode:

Каждый файл или каталог имеет **уникальный inode-номер**.  
В нём записано:
- 🔸 **Тип файла** (обычный, каталог, симлинк, сокет и т.д.)
- 🔸 **Права доступа** (rwx — для owner, group, others)
- 🔸 **Владелец (UID)** и **группа (GID)**
- 🔸 **Размер файла**
- 🔸 **Временные метки**:
    - `atime` — последний доступ
    - `mtime` — последняя модификация
    - `ctime` — последнее изменение метаданных
- 🔸 **Количество жёстких ссылок**
- 🔸 **Указатели на блоки данных**, где реально лежит содержимое файла

`ls -i` - покажет inode

`stat notes.txt`
```yaml
  File: notes.txt
  Size: 1024       Blocks: 8          IO Block: 4096 regular file
Device: 803h/2051d Inode: 123456      Links: 1
Access: (0644/-rw-r--r--)  Uid: (1000/user)   Gid: (1000/user)
Access: 2025-10-22 08:15:00
Modify: 2025-10-22 08:14:59
Change: 2025-10-22 08:15:00
```

#### 🔹 Почему это важно DevOps инженеру:

- При администрировании **файловых систем** (ext4, xfs и т.д.) важно понимать, что:
    - Файловая система может закончить **inode'ы** даже если есть место на диске — тогда новые файлы не создаются.
    - При удалении файлов inode освобождается.
    - При копировании сохраняются новые inode, а при **жёсткой ссылке** (`ln`) — inode общий.

---

## Почему inode может закончиться

Каждый файл в Linux — **требует хотя бы один inode**.  
Когда ты **создаёшь файловую систему (например, ext4)**, количество inode создаётся **заранее**, исходя из ожидаемого количества файлов.

🔹 Например:
```bash
mkfs.ext4 -i 16384 /dev/sda1
```
`-i` - означает один inode на каждые 16 384 байта
```bash
mkfs.ext4 -N 10000000 /dev/sda1
```
^ задает кол-во inode нарпямую

