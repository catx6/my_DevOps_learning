fs - способ организации хранения и управления файлами на диске
**`в LINUX все это файл`**

----

|Тип|Где используется|Особенности|
|---|---|---|
|**ext4**|основная ФС для Linux|стабильная, поддержка больших файлов|
|**xfs**|сервера, базы данных|высокая производительность|
|**btrfs**|DevOps, Docker, backup|снапшоты, сжатие, клонирование|
|**tmpfs**|в оперативной памяти|временные файлы, `/run`, `/tmp`|
|**vfat/exfat**|флешки, совместимость с Windows|без прав доступа|
|**nfs**|сетевые хранилища|доступ по сети (часто с Prometheus/Grafana)|

---

| Каталог | Назначение                                                          |
| ------- | ------------------------------------------------------------------- |
| `/`     | корень всей системы                                                 |
| `/bin`  | основные команды (`ls`, `cp`, `mv`, `cat`)                          |
| `/sbin` | системные команды для root                                          |
| `/etc`  | **конфиги** системных служб (nginx, ssh, systemd, docker)           |
| `/home` | домашние каталоги пользователей                                     |
| `/root` | домашний каталог суперпользователя                                  |
| `/var`  | логи, временные данные, spool-файлы (`/var/log`, `/var/lib/docker`) |
| `/tmp`  | временные файлы                                                     |
| `/usr`  | приложения и библиотеки                                             |
| `/opt`  | сторонние программы                                                 |
| `/dev`  | устройства (например `/dev/sda`, `/dev/null`)                       |
| `/proc` | информация о процессах и ядре                                       |
| `/sys`  | параметры ядра и устройств (современный аналог `/proc`)             |
| `/run`  | временные runtime-файлы, PID-файлы демонов                          |

------

### ключевые команды для фс
- lsblk - просмотр дисков
- df -h -выводит размер и заполненость
- du -sh /var/log - выводит размер папки
- mount /dev/sdb1/ /mnt/data - подключить диск
- размонтировать umount /mnt/data
- mkfs.ext4 /dev/sdb1 - создать ФС ext4
- fsck /dev/sdb1 - проверка\починка ошибок ФС
- chmod chown umask - работа с правами
- tail -f /var/log/syslog - мониторинг
- ln -s /var/log /logs - символическая ссылка

----

## 🐳 7. В контексте DevOps

- **Docker** монтирует файловую систему контейнера → `/var/lib/docker`
- **Kubernetes** использует **Volume’ы**, **PersistentVolumeClaim**
- **CI/CD** (GitLab, Jenkins) часто создают временные файлы → `/tmp`, `/var/lib/jenkins`
- **Backup / Snapshot** — через `rsync`, `btrfs snapshot`, `tar`, `restic`
- **Безопасность** — через правильные права и SELinux/AppArmor

-----

## Дополнительно
новосозданная папка имеет указатель на предыдущую и текущую директорию ./ и ../
ls -a выведет их
файлы которые начинаются на точку в unix/linux являются скрытыми