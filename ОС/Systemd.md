systemd - это современная **`программа инициализации`** и **`менеджер сервисов`** в LINUX
- Systemd отвечает за запуск и управление и завершение всех процессов и служб после перезагрузки ядра
### Задачи Systemd
1. инициализация системы 
	после перезагрузки ядра он будет первым процессом([[processes in LINUX]]), далее он стартует все службы монтирует файловые системы и активирует все сетевые интерфейсы
2. Управление сервисами
	заменяет старые init системы (SysVinit, upstart) и управляет службами через юнит файлы `.service`, `.socket`, `.mount`, `.target`
3. парарельный запуск и отслуживание зависимостей
4. мониторинг и перезапуск
5. журналирование и логирование
6. управление сессиями
7. планировка задач
8. монтирование дисков
9. управление сокетами([[СЕТИ]])
10. управление устройствами
## 📂 Основные типы юнитов

|Тип юнита|Расширение|Назначение|
|---|---|---|
|Service|`.service`|Управление демонами и службами|
|Socket|`.socket`|Сокеты, которые запускают сервис при подключении|
|Target|`.target`|Группы юнитов (например, `multi-user.target`)|
|Mount|`.mount`|Монтирование файловых систем|
|Timer|`.timer`|Планирование задач (аналог cron)|
|Device|`.device`|Управление устройствами|
|Path|`.path`|Реакция на изменение файлов|
## 🧠 Основные команды (через `systemctl`)

| Команда                               | Описание                              |
| ------------------------------------- | ------------------------------------- |
| `systemctl start nginx`               | Запустить службу                      |
| `systemctl stop nginx`                | Остановить                            |
| `systemctl restart nginx`             | Перезапустить                         |
| `systemctl enable nginx`              | Автозапуск при старте системы         |
| `systemctl disable nginx`             | Убрать из автозапуска                 |
| `systemctl status nginx`              | Проверить статус                      |
| `systemctl list-units --type=service` | Показать все активные службы          |
| `systemctl daemon-reload`             | Перечитать юнит-файлы после изменений |
### логи
```bash
journalctl -u nginx.service #логи конкретной службы
journalctl -xe #последние ошибки
journalctl show nginx #подробная информация об юните
```

## journald

`systemd-journald` - встроеная система централизованого логирования в Linux и собирает все сообщения от
- системных служб systemd
- ядра linux
- демонов и приложений
- пользователей через logger
- boot-процессов
Все сообщения сохраняются в **бинарных журналах** что делает возможным
- быстрый поиск и фильтрацию
- структурированые поля
- безопасное хранение
-----
#### хранилище логов
- `/run/log/journal/` -> временные логи исчезают после перезагрузки
- `/var/log/journal/` -> постоянные логи
Если директории `/var/log/journal/` нет, можно создать:
```bash
sudo mkdir -p /var/log/journal
sudo systemd-tmpfiles --create --prefix /var/log/journal
sudo systemctl restart systemd-journald
```

-----
###  Основные команды `journalctl`

### 🔹 Общие примеры

| Команда                                 | Описание                                       |
| --------------------------------------- | ---------------------------------------------- |
| `journalctl`                            | Показать весь журнал                           |
| `journalctl -xe`                        | Последние ошибки с подробностями               |
| `journalctl -b`                         | Логи с момента последней загрузки              |
| `journalctl -b -1`                      | Логи предыдущей загрузки                       |
| `journalctl -u nginx.service`           | Логи конкретной службы                         |
| `journalctl -f`                         | “Хвост” — следить за логами в реальном времени |
| `journalctl --since "2025-10-20 14:00"` | Логи начиная с определённого времени           |
| `journalctl --since yesterday`          | Логи за вчера                                  |
| `journalctl -p err..alert`              | Только ошибки (priority от error до alert)     |
### уровни приоретета
**`Journald`** - использует систему приорететов

|Уровень|Код|Пример|
|---|---|---|
|`emerg`|0|Kernel panic|
|`alert`|1|Очень серьёзная ошибка|
|`crit`|2|Критическая ошибка|
|`err`|3|Обычная ошибка|
|`warning`|4|Предупреждение|
|`notice`|5|Замечание|
|`info`|6|Информационное сообщение|
|`debug`|7|Отладочная информация|

`journalctl -p 3 -b`

---
```bash
journalctl -u ssh.service
journalctl -u nginx.service -f
journalctl -u prometheus.service --since "1 hour ago"
```

----

каждая запись содержит 
- - Время (`_SOURCE_REALTIME_TIMESTAMP`)
- PID, UID, GID
- Имя юнита (`_SYSTEMD_UNIT`)
- Сообщение (`MESSAGE`)
- Hostname и другие метаданные
```bash
journalctl -o verbose -u nginx #подробный режим
```

---

### форматы вывода

-o short - короткий
-o verbose - подробный
-o json - JSON для парсинга 
-o cat - вывод текстом

```bash
sudo journalctl --vacuum-size=500M # оставить только 500 МБ
sudo journalctl --vacuum-time=2weeks #удалить старше 2 недель

```

---

### как программы пишут в журнал
1. через стандартные потоки stdout и stderr (если запущены из под systemd)
2. через команду logger `logger "backup sucess"`
 ```bash
 cat@kittycatx ~> logger "DAROVA MELLSTROY"
cat@kittycatx ~> journalctl | grep "DAROVA MELL"
Oct 20 20:38:19 kittycatx cat[7294]: DAROVA MELLSTROY
cat@kittycatx ~> journalctl -o json | grep "DAROVA MELL"
{"_CAP_EFFECTIVE":"0","__SEQNUM":"589751","_TRANSPORT":"syslog","_UID":"1000","SYSLOG_IDENTIFIER":"cat","_MACHINE_ID":"33900f2995384268b15ea7e839f1ca5d","_COMM":"logger","MESSAGE":"DAROVA MELLSTROY","_BOOT_ID":"7754fa2902be49b59adfc6f6323361cf","PRIORITY":"5","_RUNTIME_SCOPE":"system","__REALTIME_TIMESTAMP":"1760992699177680","__CURSOR":"s=29a1a21cd41c463a81a860dad19ea649;i=8ffb7;b=7754fa2902be49b59adfc6f6323361cf;m=20e4a12e7;t=6419d10495ad0;x=58e1cdeb2ff8bf14","_PID":"7294","SYSLOG_TIMESTAMP":"Oct 20 20:38:19 ","_GID":"1000","__MONOTONIC_TIMESTAMP":"8829670119","_HOSTNAME":"kittycatx","SYSLOG_FACILITY":"1","__SEQNUM_ID":"29a1a21cd41c463a81a860dad19ea649","_SOURCE_REALTIME_TIMESTAMP":"1760992699177544"}
cat@kittycatx ~> journalctl -o short | grep "DAROVA MELL"
Oct 20 20:38:19 kittycatx cat[7294]: DAROVA MELLSTROY
cat@kittycatx ~> journalctl -o cat | grep "DAROVA MELL"
DAROVA MELLSTROY
cat@kittycatx ~> 
 ```
практика

----

#### взаимодействие с `rsyslog`
Многие дистрибутивы (Debian, Ubuntu) имеют оба:
- `systemd-journald` - собирает все
- `rsyslog` - сохраняет в текстовые `/var/log/*.log`
Обычно `journald` передает копии сообщений в `rsyslog` чтобы они попадали в привычные логи типа `/var/log/syslog`

---
##### дополнительно
systemctl is-***команды***
```bash
systemctl is-active nginx
systemctl is-enabled nginx
systemctl is-failed nginx
```

### как писать сервисы (21.10.2025)

systemd-сервис это юнит файл с расширением `.service` описывающий
- что запускать `ExecStart`
- когда и при каких условиях 
- под кем
- как перезапускать логировать и т.д
```ini
[Unit]
Description=описание
After=network.target

[Service]
Type=simple
User=username
WorkingDirectory=/path/
ExecStart=/path/to/bin
Restart=on-failure
RestartSec=5s
Enviroment="ENV=production" "PORT=8080"

[Install]
WantedBy=multi-user.target
```

#### [Unit]
|Директива|Назначение|
|---|---|
|`Description`|Краткое описание службы|
|`After=`|Запускать **после** указанных юнитов (например, `network.target`)|
|`Requires=`|Обязательно требует запущенного юнита|
|`Wants=`|Желательно, но не обязательно зависимый юнит|

### Service

|Директива|Назначение|
|---|---|
|`Type=`|Режим запуска: `simple`, `forking`, `oneshot`, `notify`, `idle`|
|`ExecStart=`|Команда для запуска|
|`ExecStop=`|Команда при остановке|
|`ExecReload=`|Команда при перезагрузке|
|`Restart=`|Когда перезапускать: `always`, `on-failure`, `never`|
|`RestartSec=`|Задержка перед перезапуском|
|`User=`|Под каким пользователем запускать|
|`WorkingDirectory=`|Рабочая директория|
|`Environment=`|Переменные окружения|
|`StandardOutput=` / `StandardError=`|Куда писать вывод (например, `journal`)|
### [Install]

| Директива   | Назначение                                                                  |
| ----------- | --------------------------------------------------------------------------- |
| `WantedBy=` | Указывает, к какому таргету привязан сервис (например, `multi-user.target`) |
| `Alias=`    | Альтернативное имя сервиса                                                  |

#### TYPE
| Тип       | Поведение                             | Пример                |
| --------- | ------------------------------------- | --------------------- |
| `simple`  | Основной процесс — это `ExecStart`    | Flask, Node.js        |
| `forking` | Демон сам форкается в фоне            | Nginx, MySQL          |
| `oneshot` | Однократные задачи (backup, init)     | backup.sh             |
| `notify`  | Процесс сообщает systemd о готовности | systemd-aware сервисы |
| `idle`    | Запускается, когда система в покое    | фоновая аналитика     |