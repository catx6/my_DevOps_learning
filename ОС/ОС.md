ос - это операционная система которая обеспечивает удобный и безопасный доступ к ресурсам - процессора памяти диску сети IO-устройствами

### ⚙️ Основные функции операционной системы:

1. **Управление процессами** — кто сейчас выполняется, кто ждёт, сколько CPU времени дать каждому [[Systemd]]
    → Планировщик процессов (scheduler).

2. **Управление памятью** — выделяет и освобождает оперативную память для программ.  
    → Например, когда запускаешь приложение — ОС решает, какие данные держать в RAM, а какие выгрузить на диск.

3. **Управление файлами и дисками** — создание, удаление, чтение файлов; работа с файловыми системами (ext4, NTFS, FAT32 и т.д.).

4. **Управление устройствами (драйвера)** — ОС общается с клавиатурой, сетевой картой, видеокартой и т.п.

5. **Управление пользователями и безопасностью** — права доступа, пользователи, группы, root / admin [[users & groups]]

6. **Сеть** — реализация [[TCP]]/[[IP]]-стека, маршрутизация, сокеты и т.п.

# структура ОС:
1. ядро(kernel) - "сердце" системы и работает с железом напрямую
2. системные библиотеки - интерфейсы для программ (glibc, systemd)
3. системные утилиты - команды и службы (bash ssh ls journald)
4. пользовательский интерфейс (user space) - терминал окна gui и все что видит user

## что мне придется изучить в ОС как для DevOps
### основа
1. что такое ядро
2. [[syscalls]] 
3. [[процессы]]
4. [[потоки]]
5. как ОС управляет памятью процессором и файлами
6. как работает загрузка системы **([[Boot]])**

### 2. Работа в Linux
1. навигация в файловой системы(FS)
2. Управление пользователями и правами доступа (`sudo`, `adduser`, `groups`, `chmod`, `umask`)
3. Работа с **службами и процессами** (`systemctl`, `journalctl`, `service`, `ps aux`,)
4. Понимание структуры `/etc`, `/var`, `/usr`, `/proc`, `/dev`
5. Мониторинг ресурсов (`top`, `vmstat`, `iotop`, `free`, `netstat`, `ss`)
6. Логи и логирование (`/var/log`, `syslog`, `journalctl`)
#### 3. Сетевой стек
1. Как работает стек TCP/IP в Linux
2. настройка IP,маршрутов и DNS
3. работа с сокетами и портами
4. Firewall и NAT (iptables, nftables, ufw)
5. работа с `/etc/hosts` `/etc/resolv.conf`
#### 4. файловые системы
1. Что такое [[inode]], mount, fstab
2. управление разделами (`fdisk`, `lsblk`, `mount`, `umount`)
3. LVM RAID(на базовом уровне)
4. Права доступа и атрибуты(`chmod`, `chown` `umount`)

### 5. Управление процессами и производительностью

- Как работает **планировщик процессов**
    
- Приоритеты (`nice`, `renice`)
    
- Просмотр зависаний (`dmesg`, `strace`, `lsof`)
    
- Оптимизация производительности — CPU, RAM, I/O, сеть
    

---

###  6. Systemd и init

- Что такое **systemd**, **unit-файлы**, **targets**
    
- Создание и отладка сервисов (`systemctl status`, `journalctl -u service`)
    
- Разница между systemd и другими init-системами (SysVinit, Upstart)
    

---

###  7. Безопасность и права

- Понимание **root и sudo**
    
- Контроль доступа: файлы, процессы, sudoers
    
- Основы SELinux/AppArmor
    
- Безопасные настройки SSH
    
- Аудит логов
    

---

### 🐧 8. Bash и Shell scripting

DevOps должен уметь **писать скрипты для автоматизации:**

- Bash синтаксис, переменные, условия, циклы
    
- Пайпы (`|`), перенаправления (`>`, `>>`, `2>&1`)
    
- cron и systemd timers для задач по расписанию
    

---

### 🧠 9. Контейнеризация и ядро

(ближе к продвинутому уровню)

- Пространства имён (namespaces) и контрольные группы (cgroups)
    
- Как Docker использует механизмы ядра Linux
    
- Изоляция процессов
    

---

### 📈 10. Мониторинг и отладка

- Метрики системы: `vmstat`, `iostat`, `sar`, `dmesg`
    
- Отладка зависаний или высокой нагрузки
    
- Чтение логов ядра и служб


## План изучения для DevOps:

1. [[утилиты]]
2. **Процессы, systemd, users**
3. **Файловые системы**
4. **Сеть в Linux**
5. **Bash скрипты**
6. **Безопасность и SSH**
7. **Мониторинг и отладка**
8. **Контейнеризация и namespaces**