netstat - сетевая утилита показывающая все соединения и прослушиваемые порты
`netstat -tulnp`
`-t` - TCP
`-u` - UDP
`-l` - Listening
`-n` - Numeric(только ip без днс имен)
`-p` - Process(процесс PID) 