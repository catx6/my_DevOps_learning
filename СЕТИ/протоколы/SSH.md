**SSH (Secure Shell)** - сетевой протокол позволяющий безопасно подключаться к удаленым серверам и управлять ими через шелл

### как работает
1. Клиент подключается к севреру 
2. происходит обмен ключами - создается безопасный канал
3. после этого можно работать
	- выполнять команды
	- копировать файлы scp sftp
	- тунелировать порты
```ssh
ssh hostname@ip
```

#### SSH-ключи
Для входа без пароля применяются пары ключей 
- открытый ключ (id_rsa.pub) - хранится на сервере
- закрытый ключ (id_rsa) - хранится у клиента
Аутенфикация проходит по совпадению ключей

**конфигурация**
- файл сервера: `/etc/ssh/sshd_config`
- файл клиента: `~/.ssh/config`
##### примеры использования
копирование файла: `scp file.txt user@host:/path/`
передача директорий: `rsync -avz ./project user@host:/srv/`

## настройка входа без пароля
`ssh-keygen -t rsa -b 4096 -C "email"`
	🔹 `-t rsa` — тип ключа (можно `ed25519`, он современнее и быстрее)  
	🔹 `-b 4096` — длина ключа (чем больше, тем безопаснее)  
	🔹 `-C` — комментарий (удобно для идентификации)
```bash
~/.ssh/id_rsa <- закрытый ключ оставлять на машине у себя
~/.ssh/id_rsa.pub <- открытый ключ скопировать на хост куда входить
```
```ssh
ssh-copy-id user@host
```

### Вариант 2 (вручную, если ssh-copy-id нет):

1. Скопируй содержимое файла:
2. cat ~/.ssh/id_rsa.pub
3. На сервере создай (если нет) каталог:
4. mkdir -p ~/.ssh && chmod 700 ~/.ssh
5. Добавь ключ в файл:
6. echo "вставь_сюда_свой_ключ" >> ~/.ssh/authorized_keys
7. chmod 600 ~/.ssh/authorized_keys
