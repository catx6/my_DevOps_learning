mac адресс(L2) - media acess control address - это уникальный физ адрес сетевого устройства который присваивается сетевой карте(L1) на уровне оборудования(NIC) 
он используется для индефикации устройств в сети LAN

### основное
- длина 48 бит (6 байт)
- `00:1A:2B:3C:4D:5E`
- определяет кому отправить пакет в пределах одной сети или wifi
### struct
первые 3 байта(OUI) - это идентификатор производителя `00:1A:2B`
последние 3 байта - уникальный номер устройства у производителя `3C:4D:5E`

### как посмотреть MAC

`ip link show`
`ifconfig`

----

### 🧠 Важно:

- MAC-адрес работает **на уровне канального слоя (Layer 2)** модели **OSI**.
- В отличие от **IP-адреса (Layer 3)** — он **не маршрутизируется** и **действителен только в пределах локальной сети**.
- Можно **изменить (подменить)** MAC (это называется _MAC spoofing_), например:
```bash
sudo ip link set dev eth0 address 00:11:22:33:44:55
```